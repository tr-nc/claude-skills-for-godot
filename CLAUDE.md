# Commit Style

This repository uses a simple, descriptive commit message style:

- For additions: `add <description>`
  - Example: `add new docs`

- For fixes: `fix <description>`
  - Example: `fix window management bugs`

Keep commit messages concise and descriptive.
